[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: 需真实 LLM API 的端到端测试（默认 CI 不跑，本地加 -m integration）
filterwarnings =
    ignore::DeprecationWarning:starlette.testclient
