# 切片 12 · 执行手册（拍照理解 + Agent 编排）

> **配套**：[切片12-拍照理解型VLM与Agent编排](./切片12-拍照理解型VLM与Agent编排.md)
> **状态**：**✅ 已关闭**（2026-06-23）
> **前置**：切片11（`classify_photo` + 家长学情页）· 切片08（理解型 VLM 已验证）

---

## 验证结论摘要

| # | 标准 | 结果 |
|---|------|------|
| 1 | 批改卷理解 + is_correct | ☑ |
| 2 | 无上传即 ingest | ☑ |
| 3 | Vision 卡片 + 留空输入框 | ☑ |
| 4 | 复盘语义 → classify_photo / photo_auto | ☑ |
| 5 | 讲解不整页 classify | ☑ |
| 6 | 入库按批改；口播可验算纠错 | ☑（设计取舍，见假设卡 §5） |

**关键证据**：`vis-20260623-094116` · 09:42 批量 17 条 `photo_auto` · 多轮后手动「记进学情」仍带 vision。

---

## 0. 验证前检查（归档）

```bash
cd /mnt/c/Users/Administrator/Desktop/agent_community
export PATH=$HOME/.hermes/hermes-agent/venv/bin:$PATH
PY=$HOME/.hermes/hermes-agent/venv/bin/python

hermes doctor   # ✓ agent_student
$PY -m agent_platform.perception.smoke_vision_session   # 4/4 无 API
```

**孩子聊天服务**（8771）：

```bash
$PY -m uvicorn agent_platform.api.student_chat:app --host 127.0.0.1 --port 8771
```

浏览器：**http://127.0.0.1:8771/**

---

## 1. 路径 A：批改卷 + 记学情意图

1. 📷 选已批改数学卷 → Vision 卡片（非 OCR 灌输入框）
2. 点 **「帮我把错题记进学情」** 或多轮后手动输入同义句
3. CLI：`attempt list g2-stu-01 --limit 5` → 最新 `source=photo_auto`

---

## 2. 路径 B：讲解意图

「讲讲我做错的题」/「这道我不会」→ 讲解为主，不整页 `classify_photo`。

---

## 3. 路径 C：空白题

未批改单题 → 讲解，无 `photo_auto`。

---

## 4. 已知问题与修复（已合入）

| 现象 | 修复 |
|------|------|
| 多轮对话后只记单题 freeform | 发送后保留 `pendingVision`，直至点 × 或新拍照 |
| 首版误用 `/api/ocr` 丢批改 | 已切 `/api/vision/understand` |

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-23 | 初稿 |
| 2026-06-23 | vision 多轮保留；P7 语义意图 |
| 2026-06-23 | **关闭**：标准 6 入库/口播分层；文档归档 |
